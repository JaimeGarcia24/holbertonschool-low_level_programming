#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
char test[60] = ("\"Programming is like building a multilingual puzzle");
puts(test);
return (0);
}
