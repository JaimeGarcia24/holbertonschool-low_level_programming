#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - reverse alphabet
 *
 * Return: 0 
 */
int main(void)
{
char alphab;

for (alphab = 'z'; alphab >= 'a'; ch--)
putchar(alphab);

putchar('\n');

  return (0);
}
